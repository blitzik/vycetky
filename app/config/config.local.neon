parameters:
	mainEmail: 'admin@email.em'

	database:
		driver: mysql
		host: 127.0.0.1
		username: root
		password: asqw
		database: invoice
		lazy: true
		profiler: true

	company:
		name: 'Company name'

	emails:
		system: 'system@email.em'
		admin: %mainEmail%